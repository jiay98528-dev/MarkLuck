# 软件开发场景中文语料

排查问题时，先确认复现步骤。
排查问题时，先记录输入状态。
排查问题时，先查看失败断言。
排查问题时，先缩小最小用例。
排查问题时，先判断是否是缓存问题。
排查问题时，先确认是否是异步竞态。
排查问题时，先区分测试问题和运行时问题。
排查问题时，先保存当前观察结果。

根因是状态读取太晚。
根因是路径没有快照。
根因是候选没有降噪。
根因是模型上下文被污染。
根因是补全结果没有同一行约束。
根因是负例没有覆盖。
根因是训练报告没有暴露失败。
根因是本地缓存没有降级处理。

修复方式是保存局部快照。
修复方式是清理旧的候选。
修复方式是补充回归测试。
修复方式是限制同一光标位置。
修复方式是拒绝低价值候选。
修复方式是提高高质量语料权重。
修复方式是拆分模型和规则。
修复方式是把失败写进报告。

验证重点是用户可见结果。
验证重点是候选是否有用。
验证重点是按下 Tab 后内容正确。
验证重点是按下 Escape 后候选清空。
验证重点是切换焦点后不误接受。
验证重点是结构化补全不回退。
验证重点是负例不触发。
验证重点是性能没有明显下降。

发布前需要确认默认模型。
发布前需要确认回退模型。
发布前需要确认模型大小。
发布前需要确认训练报告。
发布前需要确认中文评分。
发布前需要确认英文评分。
发布前需要确认结构化场景。
发布前需要确认交互闭环。

配置变更需要记录原因。
配置变更需要记录影响范围。
配置变更需要记录回滚方式。
配置变更需要记录验证命令。
配置变更需要记录剩余风险。
配置变更需要避免影响普通用户。
配置变更需要保持向后兼容。
配置变更需要同步测试预期。

模型更新需要先看体积。
模型更新需要先看命中率。
模型更新需要先看准确度。
模型更新需要先看负例。
模型更新需要先看低价值上下文。
模型更新需要先看语言分布。
模型更新需要先看类别占比。
模型更新需要先看性能阈值。

候选质量需要关注语义。
候选质量需要关注长度。
候选质量需要关注语言一致。
候选质量需要关注上下文污染。
候选质量需要关注网页腔。
候选质量需要关注重复短词。
候选质量需要关注结构优先。
候选质量需要关注接受率。

软件开发笔记应该先写现象。
软件开发笔记应该再写根因。
软件开发笔记应该补充修复。
软件开发笔记应该记录验证。
软件开发笔记应该保留风险。
软件开发笔记应该避免长篇铺垫。
软件开发笔记应该方便复查。
软件开发笔记应该服务下一次排障。

变更摘要：补充软件开发场景。
变更摘要：提高英文技术短句质量。
变更摘要：压制低价值候选。
变更摘要：保持结构化补全优先。
变更摘要：扩展中文工作笔记。
变更摘要：补充散文短句边界。
变更摘要：重新生成默认模型。
变更摘要：运行补全质量测试。

剩余风险：英文语义仍可能漂移。
剩余风险：训练空间接近上限。
剩余风险：网页语料可能带来噪声。
剩余风险：负例样本还不够多。
剩余风险：真实用户历史会改变排序。
剩余风险：本地缓存损坏需要降级。
剩余风险：同一上下文需要继续观察。
剩余风险：性能阈值需要持续回归。
