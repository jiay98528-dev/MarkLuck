# 软件排障与发布短句

排查问题时先保留失败现场。
排查问题时先记录输入状态。
排查问题时先确认复现步骤。
排查问题时先缩小影响范围。
排查问题时先检查最近变更。
排查问题时先判断是否缓存污染。
排查问题时先区分规则和模型。
排查问题时先验证结构化补全。
排查问题时先观察普通文本候选。
排查问题时先确认是否跨行补全。
排查问题时先看训练报告。
排查问题时先看低价值上下文。

现象是补全出现太少。
现象是候选内容不够可用。
现象是英文候选出现漂移。
现象是中文短句命中不足。
现象是网页腔候选过多。
现象是模型空间没有用满。
现象是结构化补全优先级正常。
现象是序列规律已经可以触发。
现象是手动输入仍需要更多覆盖。
现象是负例压制没有明显回退。

根因可能是短锚点语料不足。
根因可能是 fallback 文本过弱。
根因可能是通用网页语料占比过高。
根因可能是英文技术短句太分散。
根因可能是训练片段没有形成高频上下文。
根因可能是质量门压掉了功能词开头。
根因可能是模型命中被结构化规则覆盖。
根因可能是上下文太短导致候选不稳定。
根因可能是同一触发词后续分布太散。
根因可能是报告没有暴露具体盲区。

修复方式是增加中文短锚点。
修复方式是增加英文软件笔记。
修复方式是增加发布检查短句。
修复方式是增加排障复盘样例。
修复方式是降低低价值 fallback。
修复方式是保留结构化优先级。
修复方式是继续使用同一行约束。
修复方式是避免继续抓泛网页。
修复方式是按场景分桶补语料。
修复方式是重训默认 compact 模型。
修复方式是复测中文和英文场景。
修复方式是记录模型大小和剩余空间。

验证重点是中文命中率。
验证重点是英文准确度。
验证重点是序列规律。
验证重点是结构化补全。
验证重点是负例误触发。
验证重点是模型加载速度。
验证重点是 provider 计算耗时。
验证重点是候选是否同一行。
验证重点是候选是否过长。
验证重点是候选是否混合语言。
验证重点是用户手测样例。
验证重点是报告扣分理由。

发布前检查默认模型路径。
发布前检查回退模型路径。
发布前检查 compact 文件大小。
发布前检查训练报告字段。
发布前检查语言分布。
发布前检查类别权重。
发布前检查低价值上下文。
发布前检查网页腔命中。
发布前检查中文短句。
发布前检查英文短句。
发布前检查散文随笔。
发布前检查软件开发场景。
发布前检查 E2E 评分。
发布前检查手动验收。

质量判断优先看用户可用性。
质量判断优先看短句是否自然。
质量判断优先看是否安静。
质量判断优先看是否错误跨语言。
质量判断优先看是否污染结构化补全。
质量判断优先看是否能解释扣分。
质量判断优先看真实输入节奏。
质量判断优先看常见触发词。
质量判断优先看软件开发笔记。
质量判断优先看中文日常记录。
