# Software note anchor phrases

When reviewing the patch, check the user visible behavior first.
When reviewing the patch, check the focused test result first.
When reviewing the patch, compare the report before and after training.
When reviewing the patch, inspect the provider order.
When reviewing the patch, inspect the resolver quality gate.
When reviewing the patch, inspect the default model path.
When reviewing the patch, inspect the fallback model path.
When reviewing the patch, keep unrelated changes out of scope.
When reviewing the patch, record the remaining risk.
When reviewing the patch, verify the exact manual scenario.

During implementation, keep the provider contract stable.
During implementation, keep the ghost text UI unchanged.
During implementation, keep structured completions first.
During implementation, keep normal prose suggestions short.
During implementation, keep mid line suggestions quiet.
During implementation, keep the model file below the hard cap.
During implementation, keep the raw web cache out of the release.
During implementation, keep corpus fragments independent.
During implementation, keep English anchors focused on software notes.
During implementation, keep Chinese anchors focused on daily notes.

During validation, run the focused autocomplete tests.
During validation, run the browser quality scenario.
During validation, compare hit rate and accuracy.
During validation, inspect the negative probes.
During validation, inspect the manual input regression.
During validation, inspect the sequence pattern score.
During validation, inspect the software development score.
During validation, inspect the prose and essay score.
During validation, inspect provider timing.
During validation, inspect visible suggestion latency.

After the fix, the same line constraint still holds.
After the fix, the structured provider still wins.
After the fix, the fallback text is more useful.
After the fix, the default compact model is larger.
After the fix, the report still passes hard governance.
After the fix, the model has no mixed language fragments.
After the fix, the quality suite should remain stable.
After the fix, the manual typing sample should feel quieter.
After the fix, the English software note should be more predictable.
After the fix, the Chinese short anchor should be more useful.

If the browser test fails, preserve the failing input.
If the browser test fails, preserve the visible suggestion.
If the browser test fails, compare the expected scenario.
If the browser test fails, check whether the model loaded.
If the browser test fails, check whether the fallback fired.
If the browser test fails, check whether the provider was disabled.
If the browser test fails, check whether the candidate was rejected.
If the browser test fails, rerun the focused spec.
If the browser test fails, avoid changing broad UI code.
If the browser test fails, keep the reproduction small.

Acceptance criteria are clear hit rate.
Acceptance criteria are clear accuracy score.
Acceptance criteria are no mixed language suggestions.
Acceptance criteria are low negative trigger rate.
Acceptance criteria are stable structured completions.
Acceptance criteria are sequence patterns above threshold.
Acceptance criteria are provider p90 under budget.
Acceptance criteria are visible p90 under budget.
Acceptance criteria are a compact model under six megabytes.
Acceptance criteria are a report with readable reasons.

The remaining risk is generic English web text.
The remaining risk is a short fallback hiding model quality.
The remaining risk is a candidate that starts with a weak word.
The remaining risk is a noisy source dominating a small context.
The remaining risk is a browser scenario not covered by probes.
The remaining risk is an English anchor leaking into Chinese context.
The remaining risk is a creative phrase becoming too narrative.
The remaining risk is overfitting to the test fixture.
The remaining risk is using model space without improving utility.
The remaining risk is confusing completion with long form writing.

This change keeps the UI unchanged.
This change keeps the provider facade stable.
This change keeps the local first design.
This change keeps the raw corpus local.
This change keeps the clean cache out of the package.
This change keeps the compact model as the release asset.
This change keeps the fallback model available.
This change keeps the resolver responsible for quiet behavior.
This change keeps the training report as the audit record.
This change keeps the manual scenario covered.

The quality report shows the model byte size.
The quality report shows the remaining byte budget.
The quality report shows the language distribution.
The quality report shows the category balance.
The quality report shows the skipped mixed fragments.
The quality report shows the probe summary.
The quality report shows the top contexts.
The quality report shows the low value contexts.
The quality report shows the hard limit status.
The quality report shows the soft target status.
