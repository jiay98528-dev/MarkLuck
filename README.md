# MarkLuck
MarkLuck is a lightweight, offline-first Markdown notebook. Each note is a plain .md file in a folder you own. No database lock-in. Sync via any file tool (OneDrive, Git). Native Markdown, built-in templates, export to PDF/TXT/docx/xlsx/CSV. Runs on desktop &amp; web. Your data, your rules.
